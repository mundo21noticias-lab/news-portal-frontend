/**
 * ⚠️ ARCHIVO DEPRECADO - REMOVIDO
 *
 * Este archivo contenía datos falsos/mock que ya no se utilizan.
 * Todos los datos ahora provienen de Firebase en tiempo real.
 *
 * MIGRACION DE DATOS:
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 *
 * Tipos (NewsArticle, Category):
 *   → lib/types.ts
 *
 * newsArticles → getArticles()
 *   → lib/firebase-service.ts
 *   → Fetch automático de Firebase
 *
 * opinionArticles → getArticles() (filtrado)
 *   → lib/firebase-service.ts
 *   → Se filtran en app/page.tsx
 *
 * categories → getCategories()
 *   → lib/firebase-service.ts
 *   → Fetch automático de Firebase
 *
 * formatDate(), formatRelativeTime():
 *   → lib/types.ts
 *
 * ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 *
 * POR FAVOR NO IMPORTES DESDE AQUÍ
 * Este archivo está vacío intencionalmente para prevenir
 * el uso accidental de datos falsos.
 *
 * Lee TRACKING_VIEWS_GUIDE.md y SEO_VALIDATION_GUIDE.md
 * para más información sobre la arquitectura actual.
 */

// Archivo vacío intencionalmente.
// No importes desde aquí.

